# OOP Use

- Don’t over-engineer with heavy class hierarchies; prefer simple functions and light data containers.
- Use classes when state/behavior belong together; avoid “Java-style” patterns in Python.
- Keep classes small and focused; composition over inheritance.

