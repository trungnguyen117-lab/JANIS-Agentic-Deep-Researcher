# Docstrings

- Public functions/classes/methods require docstrings (Google style is fine).
- Document purpose, args, returns, and important side effects or exceptions.
- Keep them short and actionable; avoid duplicating obvious type hints.

